module down_counter #(parameter N = 4)(input clk, reset, enable, output reg [N-1:0] count);
    always @(posedge clk or posedge reset) begin
        if (reset)
            count <= 0;
        else if (enable)
            count <= count - 1;
    end
endmodule